#! /usr/bin/python

import os
import argparse

#pull in list of names pulled from fasta file

#replace '>' with '' in names

#create a dict from names, with the accession #'s as keys for each name

#If line contains [accession #], replace with value from key


